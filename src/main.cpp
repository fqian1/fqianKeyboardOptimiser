#include <iostream>
#include <vector>
#include <cmath>
#include <map>

struct Key
{
    double x, y;
    int finger;

    Key(double x, double y, int finger) : x(x), y(y), finger(finger) {}
};

class Finger
{
public:
    double distance
}